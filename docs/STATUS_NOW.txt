Bot A — Where We Are (simple words)

What is working
- The bot loop is running as a single process.
- The watchdog is running every 5 minutes and checking the loop.
- The guard can start the loop if needed.

What is NOT working (why there are no signals)
- Data fetch is failing. We see “providers failed” or “NoneType” errors.
- Alpha Vantage says “rate limit” (too many calls on the free plan).
- Yahoo payload/fields are not matching what the parser expects.
- TwelveData key exists, but the code is not using TwelveData first,
  so good data is not even tried most of the time.

Why this makes “WAIT 0 score” with age=999.9
- No valid candles = the analyzer has nothing to measure.
- With no numbers, the bot stays safe and prints WAIT with zero score.

What we are fixing first
1) Force provider order to: twelve_data,yahoo,alpha_vantage
2) Prove TwelveData returns real candles (fast curl test).
3) Make the Python runner handle “None” safely and pick TwelveData first.

After these 3, signals should come back (when market/timeframe has data).
